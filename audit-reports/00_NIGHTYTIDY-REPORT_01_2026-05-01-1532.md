# NightyTidy Report — 2026-05-01

Overnight I worked through your project end to end and gave it a thorough tidying — 44 separate passes across roughly 80 hours of careful work. The biggest wins are in safety, speed, trust, and cost.

I expanded your safety net of automatic checks from 42 to 450 — more than ten times as many. That means anytime someone changes the code in the future, hundreds of small tests will run and catch breakage before it ever reaches your users. I also wrote a "smoke check" — seven critical tests that run in under a second — so after any deploy you'll know within seconds whether the basics still work.

I cut the size of the code your visitors have to download by nearly half (from 151 KB squeezed down to 76 KB squeezed). The site loads dramatically faster now, especially on phones. Part of that came from being smart about when to load less-important things — the analytics tool now waits until after the page is up before quietly turning itself on, instead of slowing down the first paint. I also shrunk the three example posters on your homepage from 6.85 MB down to 515 KB by serving modern image formats. That's a 92.5% reduction and saves people's data plans.

I cut your AI bill by roughly 73% by turning on a caching feature that lets Claude reuse the long instructions you give it instead of paying for them every single time. At a thousand posters a month that's about $3.60 saved; at a hundred thousand it's about $360.

I made the site genuinely usable for people relying on screen readers — adding proper labels, live announcements when posters are loading, and visible focus rings for keyboard users. I closed a sneaky double-click bug where clicking Generate could secretly fire twice and double-charge you for AI calls. I also caught a bug where if your photo host stalled, users would be stuck staring at a blank square forever with no way out — now they get a clear error and can try again.

I added a new health-check web address so monitoring tools can confirm the site is alive without spending AI money on every check. I wrote 12 plain-English playbooks for what to do when things go wrong — Claude is down, costs spike, the database is unreachable. And I tagged every request with a unique ID so when a user reports an issue you can actually find their session in the logs.

Everything went smoothly — all 44 passes finished without a single failure.

---

## Run Summary

- **Date**: 2026-05-01
- **Duration**: 80h 52m
- **Steps completed**: 44/44
- **Steps failed**: 0
- **Branch**: nightytidy/run-2026-05-01-1532
- **Safety tag**: nightytidy-before-2026-05-01-1532
- **Total cost**: $311.12
- **Total tokens**: 361M input / 1.9M output


## Step Results

| # | Step | Status | Duration | Attempts | Cost |
|---|---|---|---|---|---|
| 1 | Documentation | ✅ Completed | 13m 15s | 1 | $7.89 |
| 2 | Test Coverage | ✅ Completed | 11m 44s | 2 | $11.58 |
| 3 | Test Hardening | ✅ Completed | 15m 07s | 1 | $5.83 |
| 4 | Test Architecture | ✅ Completed | 7m 32s | 1 | $4.87 |
| 5 | Test Consolidation | ✅ Completed | 13m 06s | 1 | $8.52 |
| 6 | Test Quality | ✅ Completed | 7m 26s | 1 | $2.70 |
| 7 | Test Efficiency | ✅ Completed | 10m 40s | 1 | $3.83 |
| 8 | Integration & Boundary Testing | ✅ Completed | 15m 16s | 1 | $7.85 |
| 9 | API Design & Consistency | ✅ Completed | 17m 45s | 1 | $8.07 |
| 10 | Security Sweep | ✅ Completed | 15m 25s | 1 | $9.81 |
| 11 | Dependency Health | ✅ Completed | 10m 34s | 1 | $5.39 |
| 12 | Codebase Cleanup | ✅ Completed | 10m 51s | 1 | $6.06 |
| 13 | Cross-Cutting Concerns | ✅ Completed | 9m 02s | 1 | $5.94 |
| 14 | Date/Time Handling | ✅ Completed | 11m 31s | 1 | $6.35 |
| 15 | File Decomposition | ✅ Completed | 4m 02s | 1 | $1.34 |
| 16 | Code Elegance | ✅ Completed | 13m 56s | 1 | $8.58 |
| 17 | Function Centralization Discovery | ✅ Completed | 10m 07s | 1 | $4.73 |
| 18 | Architectural Complexity | ✅ Completed | 7m 34s | 1 | $4.78 |
| 19 | Scar Tissue Analysis | ✅ Completed | 3m 08s | 1 | $1.03 |
| 20 | Default Values & Magic Constants | ✅ Completed | 16m 00s | 1 | $10.21 |
| 21 | Type Safety | ✅ Completed | 14m 21s | 1 | $10.13 |
| 22 | Contract & Schema Drift | ✅ Completed | 15m 10s | 1 | $10.65 |
| 23 | Logging & Error Message | ✅ Completed | 12m 14s | 1 | $5.99 |
| 24 | Data Integrity | ✅ Completed | 14m 21s | 1 | $8.91 |
| 25 | Performance | ✅ Completed | 13m 25s | 1 | $8.02 |
| 26 | Cost & Resource Optimization | ✅ Completed | 16m 01s | 1 | $10.79 |
| 27 | Error Recovery | ✅ Completed | 13m 31s | 1 | $8.27 |
| 28 | Resource Lifecycle & Cleanup | ✅ Completed | 11m 37s | 1 | $6.17 |
| 29 | Race Condition Audit | ✅ Completed | 12m 00s | 1 | $5.33 |
| 30 | Idempotency & Safe Retry | ✅ Completed | 13m 00s | 1 | $5.79 |
| 31 | Bug Hunt | ✅ Completed | 19m 08s | 1 | $11.85 |
| 32 | Implicit Ordering & Hidden Dependencies | ✅ Completed | 5m 50s | 1 | $2.03 |
| 33 | External Integration Reliability | ✅ Completed | 18m 04s | 1 | $10.37 |
| 34 | Frontend Quality | ✅ Completed | 14m 49s | 1 | $9.06 |
| 35 | UI/UX Audit | ✅ Completed | 17m 46s | 1 | $11.19 |
| 36 | State Management | ✅ Completed | 13m 46s | 1 | $6.48 |
| 37 | Perceived Performance | ✅ Completed | 23m 48s | 1 | $13.80 |
| 38 | DevOps | ✅ Completed | 12m 31s | 1 | $8.70 |
| 39 | Scheduled Job & Cron Jobs | ✅ Completed | 11m 30s | 1 | $5.19 |
| 40 | Observability | ✅ Completed | 25m 47s | 1 | $15.58 |
| 41 | Backup Check | ✅ Completed | 4m 10s | 1 | $1.99 |
| 42 | Product Polish & UX Friction | ✅ Completed | 5m 45s | 1 | $2.69 |
| 43 | Feature Discovery & Opportunity | ✅ Completed | 6m 07s | 1 | $2.24 |
| 44 | Strategic Opportunities | ✅ Completed | 6m 26s | 1 | $4.56 |


## NightyTidy Action Plan

> Generated from a 44-step improvement run. Items below have been verified as **not yet implemented** in the current codebase.

### Recommended Refactors

These improvements have clear implementations and can be done automatically in a future run.

#### Critical
<!-- Security vulnerabilities, data loss risks, breaking bugs -->

- **Populate the empty hate-speech filter list**: `src/server/slur-list.ts` is currently `export const slurList: string[] = []` with a "must be replaced before launch" placeholder comment. The slur filter at `generate.ts` therefore returns false on every input — moderation is unenforced today. Source from LDNOOBW or a focused hate-speech subset. Value: blocks slur prompts before they reach Claude, a stated launch requirement. Impact: backend safety pipeline. Risk: Low (drop-in array).
- **Add the missing social-share preview image**: `index.html` references `/og-hero.png` and `/apple-touch-icon.png` but neither exists in `public/`. Every Slack/Twitter/iMessage paste of the URL renders a broken image. Render one canonical poster with the existing compositor, save as PNG, commit. Value: every shared link looks intentional rather than broken. Impact: brand on social. Risk: Low (static asset).
- **Fix case-sensitive bypass in real-person filter**: `POSSESSIVE_NAME_PATTERN` in `src/server/safety.ts:28` requires lowercase `\bmy` — "**My** boss Karen is loud" (the natural sentence-start capitalization) bypasses the filter entirely. Add the `i` flag to the regex. Value: closes a real moderation bypass found by the bug hunt. Impact: backend safety filter. Risk: Low (one-character change, expand existing tests).

#### High
<!-- Reliability, performance, error handling, code quality gaps -->

- **Add an automated test-running workflow on pull requests**: No `.github/workflows/` directory exists. Currently `npm run build` runs photo validation + typecheck + Vite build but never `npm test`, so a PR with broken tests would deploy. Create `.github/workflows/ci.yml` running `npm test`, `npm run typecheck`, and `npm audit --audit-level=high --omit=dev` on every PR. Value: catches breakage before deploy; closes the trust gap on agent-claimed test passes. Impact: build/deploy. Risk: Low.
- **Move runtime packages out of devDependencies**: `firebase-admin` and `@anthropic-ai/sdk` are declared in `devDependencies` despite being imported by the deployed Netlify function. This currently masks 10 transitive `npm audit` findings under `--omit=dev`. Move both to `dependencies` in `package.json`. Value: accurate vulnerability visibility; deploy robustness. Impact: package metadata. Risk: Low (re-deploy verification needed).
- **Add `prefers-reduced-motion` support**: The pulse loading animation and Radix Dialog open/close animations have no `motion-reduce:animate-none` variant — vestibular-disorder users get full motion. Add the variant in `tailwind.config.ts` and `globals.css`. Value: accessibility compliance for users who request reduced motion. Impact: animation utilities. Risk: Low.
- **Add `ENABLE_GENERATION` kill switch**: There's no way to disable `/.netlify/functions/generate` without a deploy during an Anthropic outage or cost spike. Add an env var that returns `safe_fallback` when set to `false`, mirroring the `ENABLE_TONE_CHECK` pattern. Value: outage and cost-spike lever without redeploying. Impact: backend orchestration. Risk: Low.
- **Show `Retry-After` value in the rate-limit message**: Server returns the header but client copy says only "Try again in a bit" — users have no idea if "a bit" is 5 minutes or 50. Surface `retryAfterSec` in the inline error copy. Value: less frustration on rate-limit. Impact: client error rendering. Risk: Low.
- **Add a `reason` enum field to the blocked response**: `App.tsx:125` analytics uses `result.message.includes('people')` substring matching to distinguish slur/real-person blocks — fragile to copy edits. Add a `reason: 'slur' | 'real_person'` field to `BlockedResponseSchema`. Value: decouples analytics from copy; closes the substring-coupling bug. Impact: API contract + types. Risk: Low (additive).
- **Wire one-shot retry on transient photo CDN load failures**: `loadImage()` in `src/lib/compositor.ts` rejects on the first decode error — a single dropped packet kills the user's poster. Retry once after 250ms on `decode()` rejection (not on timeout). Value: fewer user-visible failures from network blips. Impact: frontend image loading. Risk: Low (existing test pattern).
- **Reset the distress prompt input after the modal is dismissed**: `src/App.tsx:115-121` closes the distress modal but leaves the distressing text in the input — the user could re-submit it. Clear the prompt on `handleDistressContinue`. Value: closes a bad-flow corner of the safety surface. Impact: state machine. Risk: Low.

#### Medium
<!-- Maintainability, test coverage, architectural improvements -->

- **Replace volatile `Timestamp.now()` mock in `generate-contract.test.ts`**: Lines 42-47 use the unstable `now: () => ({ toMillis: () => Date.now() })` shape that `CLAUDE.md` warns against. Currently safe (contract tests don't read TTL math) but a 1-in-5 flake source the moment any contract test asserts `expiresAt - windowStart`. Copy the stable pattern from `generate-integration.test.ts:26-35`. Value: closes latent test flake. Impact: one test file. Risk: Low.
- **Reset `canvasReady` flag on phase exit in `PosterReveal.tsx:19-25`**: When leaving `settled` phase on regenerate, the flag isn't reset — `scrollIntoView` fires immediately on the new `settled` transition before the new canvas decodes, scrolling users to a blank target. Add `useEffect(() => { if (state.phase !== 'settled') setCanvasReady(false); }, [state.phase])`. Value: smoother regenerate visual flow. Impact: one component. Risk: Low.
- **Trim trailing slash in `getPhotoUrl` base URL normalization**: `src/lib/photos.ts` produces `//` in URLs when `VITE_FIREBASE_STORAGE_BASE_URL` ends in `/`. Firebase tolerates it but a future stricter CDN won't. Add `(...).replace(/\/$/, '')`. Value: defends against future host migrations. Impact: one helper. Risk: Low.
- **Narrow `fittingRung` type to `1 | 2 | 3`**: Type allows `4` but no photo-selection code path emits it; `4` is set client-side only on `safe_fallback`. Either narrow the type or split the field cleanly into `fittingRung` (server) and `isSafeFallback` (client). Value: removes type-vs-runtime drift; clearer analytics. Impact: types + client. Risk: Low.
- **Add `unhandledrejection` and `error` handlers to `main.tsx`**: Async errors that escape React's tree currently surface only in browser DevTools. Wire them through `track('frontend_error', { kind, error })` so they show up alongside other PostHog events. Value: production observability for client crashes. Impact: bootstrap + analytics. Risk: Low.
- **Extract a `useGenerateHandler` hook from `App.tsx:handleGenerate`**: 60-line orchestrator handles 8 status branches with three identical revert ternaries. Step 17 flagged it; step 16 deferred it pending component test infrastructure. Value: testable state machine; cleaner branch handling. Impact: largest component file. Risk: Medium.
- **Adopt a small `knip` or `depcheck` step in CI**: Two NightyTidy passes have now found unused-but-declared packages (`react-hook-form`, `@testing-library/*`, `Preset` type alias). Catches future "declared but unused" entries automatically. Value: prevents recurring waste pattern. Impact: CI config. Risk: Low.
- **Add Sentry/Bugsnag for client-side errors**: `error_boundary`, `gen_client_error`, `download_failed`, and `poster_render_failed` events are observable today only with browser DevTools open. Value: client crash visibility. Impact: bootstrap + analytics. Risk: Low.
- **Capture Anthropic's `_request_id` in `gen_anthropic_error`/`gen_ok` log lines**: Cross-references our trace with theirs for incident triage. Value: faster Anthropic-incident root-cause. Impact: server log shape. Risk: Low.

#### Low
<!-- Polish, style, minor optimizations -->

- **Promote the CSP header from `Report-Only` to enforced**: After a production observation window, change `Content-Security-Policy-Report-Only` to `Content-Security-Policy` in `netlify.toml`. Already documented as a follow-up by the security audit. Value: active XSS blocking. Impact: response headers. Risk: Medium (needs report-endpoint observation first).
- **Submit HSTS preload list entry**: Block downgrade attacks pre-first-visit. Submit at hstspreload.org once the team commits. Value: stronger TLS posture. Impact: header policy. Risk: Low (one-way commitment).
- **Right-edge gradient mask on the preset chip row**: After hiding the scrollbar in audit run 35/001, the overflowing chips ("M…") have no scroll affordance. Add a 24px gradient mask. Value: clearer scrollability hint. Impact: one component. Risk: Low.
- **Add About / FAQ surface**: First-time visitors see only the hero images and "What's going on?" headline. No about section, no how-it-works. Value: better cold-landing context. Impact: new component or section. Risk: Low.
- **Move remaining hardcoded labels/headings into `src/content/copy.ts`**: 26 strings outside the content module. Step 34 flagged them. Value: closes documented "no hardcoded copy" rule's drift surface; pre-stages future translation. Impact: refactor only. Risk: Low.
- **Document the Firestore TTL policy as a deploy-checklist item**: Currently noted only inside a code comment in `src/server/rateLimit.ts`. Promote to a top-level deploy checklist in `docs/RUNBOOKS.md` or `docs/CONFIGURATION.md`. Value: prevents silent free-tier collection bloat on new environments. Impact: docs only. Risk: None.
- **Code-split `PosterCanvas` and `compositor`**: ~10–20 KB gzip off initial load. The 800ms anticipation beat already masks chunk-load cost. Value: faster first paint. Impact: build config + lazy import. Risk: Low.

---

### Requires Human Review

These suggestions involve product decisions, user experience changes, or feature additions that need human judgment.

- **Replace placeholder photo library with licensed photos**: All 10 entries in `src/data/photos.json` carry `"credit": "Placeholder / Unsplash"`. Why: launch-quality brand requires real licensed photography; the curation work can't be automated. Trade-offs: Unsplash+ subscription vs. Pexels free vs. commissioned shoot vs. expanding to 50+ entries to fix the regenerate-staleness problem step 44 flagged. Effort: Medium (curation + tone matching).
- **Add native share + clipboard image copy**: P1 in `PRD/24_Future_Features.md`. The product's stated value loop is "land-to-share in under 10 seconds" but there is no share button — only Download, then user manually leaves the app. Why: closes the most-important emotional beat in the product spec. Trade-offs: `navigator.share` adoption is only ~6.6% on mobile; need a clipboard-image fallback for browsers without it; need an iOS Safari path that matches the rest of the experience. Effort: Small (~1 day total per the PRD spec).
- **Wire print-on-demand merch (Printful or similar)**: Square 1080×1080 PNGs map to 12×12" posters, mugs, sticker sheets. Why: zero monetization scaffolding exists today; Anthropic spend is one-way; InspiroBot.me's Zazzle model maps 1:1 to BYH and validates demand. Trade-offs: requires breaking the "no DB of user content" anti-feature for the user's own poster (frame as opt-in checkout, not stored gallery); requires tax/fulfillment account setup. Effort: Medium (Printful has a free Shopify integration).
- **Make `safe_fallback` distinguishable from a real `ok` response in the UI**: Today both render identically — disappointing fallback output looks like a bad joke, not "we couldn't quite land your prompt." Why: trust-loss when AI degrades. Trade-offs: explicit "we used a stock line" message vs. a more subtle visual hint vs. a different photo treatment. Effort: Small (copy + state flag, already partially in types).
- **Add a "report bad output" mechanism**: For a product that walks a sensitive line, no escape hatch exists when the AI returns something hurtful. Why: standard for sensitive AI products; protects the brand. Trade-offs: requires an inbox or review queue someone owns; risk of low-signal reports. Effort: Small.
- **Add a hotline-click analytics event**: Track whether users actually click the distress modal's hotline link or phone, distinct from "dismissed". Why: closes the loop on whether the most-important safety surface in the product helps people. Trade-offs: zero PII concern (country + kind only); just operational visibility. Effort: Small (4 lines in `DistressInterstitial.tsx`).
- **Add aspect-ratio variants (vertical 1080×1350)**: P2 in PRD. Why: unlocks Instagram Stories distribution that a square poster can't reach; the compositor and layout module already accept normalized `textZone` so adding `{ square, vertical }` keys is straightforward. Trade-offs: doubles photo-curation work for textZone tuning; complicates download UI. Effort: Medium (~1.5 days).
- **Improve the slur-block error message**: Current copy is "Let's try a different one." — doesn't tell the user what was wrong, so they re-edit blindly. Why: reduces frustration on the moderation surface. Trade-offs: how specific to be without revealing the filter list (security-through-obscurity matters here). Effort: Small (copy edit).
- **Expand hero on tablet (768–1024px)**: Mobile single-image variant currently stays active in the 488px-of-empty-space band. Why: better visual density at common iPad widths. Trade-offs: potentially fetches an extra image; only ~5% of visitors on this band per typical analytics. Effort: Small (responsive grid).
- **Wire up log-pattern alerts on a paging tool (PagerDuty/Opsgenie/Slack)**: Audit run 40/001 documented thresholds for `config_validation_failed`, `gen_anthropic_error` rate, and health 503 but has no paging integration. Why: detection speed minutes-vs-hours-on-customer-complaint when things go wrong. Trade-offs: monthly cost ($21+); requires someone on-call. Effort: Small (configuration only).
- **Set up Netlify log drain to a SIEM (Datadog/Honeycomb/New Relic)**: Real dashboards, quantile aggregation, long-term retention beyond Netlify's default. Why: structured-JSON foundation already in place from this run; SIEMs plug in natively. Trade-offs: monthly cost. Effort: Small.
- **Set Anthropic spend cap, Netlify bandwidth alert, and Firebase budget alert**: Console-side governance; this run cut costs ~73% but added no spend ceilings. Why: caps blast radius if a future change regresses caching or a runaway loop fires. Trade-offs: none — all three providers offer free alerting. Effort: Trivial (under 10 minutes total).
- **Verify Firestore TTL policy is enabled in production**: Code writes `expiresAt` correctly (3 contract tests pin it) but the TTL *policy* is a project-level Firebase console feature. If missing, the `rateLimits` collection grows forever and only billing notices. Why: invisible-from-code dependency that prior runs flagged repeatedly. Trade-offs: none. Effort: Trivial (5-minute Firebase Console check).
- **Set production env vars `IP_SALT_BASE` and `ALLOWED_ORIGINS` in Netlify**: Without `ALLOWED_ORIGINS` the CSRF shield is a no-op (cross-origin pages can drain Anthropic spend). Without `IP_SALT_BASE` the rate limiter falls back to a published default salt. Why: both are documented operator-only activations from this run's security work. Trade-offs: none. Effort: Trivial.

---

### Summary

The codebase is in genuinely strong shape — 450 tests passing, structured logging with per-request correlation, runbooks for 12 failure modes, and a 49% reduction in critical-path JavaScript. The top refactor priority is populating the empty `slur-list.ts` and adding the missing `/og-hero.png` asset before any public launch — both are flagged as ship blockers and neither requires product judgment. The most valuable human-review item is shipping native share plus clipboard image copy, which closes the gap between the product's stated "land-to-share in under 10 seconds" promise and today's download-only flow that breaks on iOS Safari.

## How to Undo This Run

If you need to reverse all changes from this run, ask Claude Code:

> "Reset my project to the git tag `nightytidy-before-2026-05-01-1532`"

Or run this git command:

```
git reset --hard nightytidy-before-2026-05-01-1532
```

The NightyTidy branch `nightytidy/run-2026-05-01-1532` is preserved and can be deleted manually when no longer needed.

---
*Generated by NightyTidy v0.1.0*
